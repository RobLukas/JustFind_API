const offersServiceMock = {
  getAllOffers: jest.fn(),
  getOffersById: jest.fn(),
  createOffer: jest.fn(),
  updateOffer: jest.fn(),
  deleteOfferById: jest.fn(),
};

export default offersServiceMock;
